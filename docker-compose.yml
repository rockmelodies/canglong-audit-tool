version: "3.9"

services:
  api:
    build:
      context: ./apps/api
    ports:
      - "9000:9000"

  web:
    build:
      context: ./apps/web
    environment:
      VITE_API_BASE_URL: http://api:9000
    ports:
      - "5173:5173"
    depends_on:
      - api
